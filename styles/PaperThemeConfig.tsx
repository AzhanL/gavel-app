import { DefaultTheme } from "react-native-paper";
export const GavelPaperTheme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: "#1f262e",
    accent: "#f1c40f"
  }
};
